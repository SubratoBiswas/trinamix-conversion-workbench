Customer defaults reach the output; target-field rules work; Customer sequence V2_2; HCM green mapping

Three separate reasons a rule change never reached the output file. Together they are why nothing an analyst changed appeared in a download, why a Fix reported success and changed nothing, and why one analyst's change was invisible to a colleague. One: the download served STALE artifacts. download-all reuses the newest file on disk when regenerate is false and never once consulted the status field, even though a stale variable was declared right there and every write path had been dutifully marking outputs stale for weeks. Staleness was recorded everywhere and read nowhere. A stale artifact is now treated exactly like a missing one — fall through and rebuild — and the single-conversion download refuses with an explanation instead of handing back a file the rules have moved on from. That also explains the two-laptop symptom precisely: there is one shared database and one shared output directory, so both analysts were served the same cached file. Two: saving a rule never marked anything stale in the first place. The rule endpoints — add, edit, delete — wrote to the database and left the artifact flagged fresh, so even a correct reuse check would have kept the old file. Every write that changes what generation would produce now marks the outputs stale: add, update and delete rule, and approve and update mapping. Three, and this is why Batch ID kept shipping 900001 and Supplier Name New kept populating even though both are seeded as blank: the analyst's correction files were not feeding the strategy overlay's BLANK set. A suppress_field learning reaches the mappings, but the control-default pass skips a column only if it is named in that blank set, and _CONTROL_DEFAULTS carries batch id equals 900001. A field with no mapping row at all — which is exactly what an unmapped Batch ID is — was therefore refilled on every generate, by design, invisibly. The 30-Jul corrections now feed the overlay, so batch id, supplier name new, inactive date, tax reporting name, procurement bu and liability distribution are all in the blank set and nothing downstream can refill them. Alongside those: a Keep blank button, because 'leave this column empty' had no way to be said. Clearing a fixed value only leaves the field OPEN, which is not the same thing — a control default or the AI refills it on the next generate, which is precisely how Batch ID kept coming back. The button now clears the source and the default, marks the mapping not_applicable so the generator suppresses it, blanks any duplicate row for the same target field (not_applicable ranks BELOW approved in the per-target dedup, so a stale sibling would have won and shipped the value while the UI said blank — the same bug one layer down), writes a suppress_field learning so every current and future conversion inherits the decision, and marks the built outputs stale because the file on disk still has the value in it. Tax Organization Type is CORPORATION and INDIVIDUAL in Oracle's own casing, in both the corrections file and the strategy defaults, with a test that fails if the two ever disagree. And the answer to why Batch ID still read 'Defaulted to 900001' on screen after all of the above: two more layers were reporting a value nothing would write. The generated file was already blank; only the UI still said 900001, which is the worse of the two failures because it makes a correct fix look broken and invites re-fixing something that is not wrong. First, compute_effective_defaults — the endpoint the Mapping Review screen reads — had no suppression check at all. It skipped a field only when that field had a source column, so an unmapped, blanked Batch ID fell straight through to the control-defaults table and came back as 900001. It now excludes every field the analyst has said must ship blank, from all three sources the generator consults: the strategy corrections, suppress_field learnings, and not_applicable mappings. The check runs before the stored default_value is read, because a stale default left on the row by an earlier seed is exactly what kept re-appearing. Second, and this is the one no server-side fix could ever have reached: the Mapping Review page carried its OWN hard-coded copy of the control-defaults table, checked BEFORE the server's answer. No rule, learning, correction or Keep blank press can touch a constant compiled into the browser bundle. That copy had also drifted — it still said Tax Organization Type equals Corporation and Business Relationship equals PROSPECTIVE, both corrected on the server weeks earlier. It is deleted. There is now one source of truth for what a field will carry, and the endpoint returns the suppressed list alongside the defaults so a blanked field reads 'Left blank' rather than silently showing nothing. A mirror that cannot be corrected is worse than no mirror. One more thing that screenshot exposed: applies_to_all_sheets was already being written into the corrections file by the analyst, and NOTHING read it. It was dead data, so two rules that say 'all sheets' were applied to exactly one sheet. Batch ID -- 'Blank on ALL sheets, a batch identifier the loader assigns is not ours to invent' -- was filed under Supplier alone, so Supplier Site, Address, Site Assignment, Contact and Bank all kept shipping 900001 and kept saying 900001 on screen. Delivery Method's CASE_WHEN never reached Supplier Site, which is where the column actually lives. The flag is now honoured, matched by PREFIX of the object name rather than as a bare wildcard: a rule filed under Supplier covers every Supplier* sheet and stops there, because Customer has a Batch ID column too and silently extending an instruction past what was said is its own kind of bug. A sheet-specific rule still beats the bundle-wide one. And the biggest one, found by measuring the file the analyst sent back rather than by reading code: THREE fields their own corrections declare BLANK were populated on every row -- Supplier Name New on all 3,872 (carrying the supplier name, which asks Fusion to rename every supplier to the name it is being created with), Procurement BU on 5,315, Liability Distribution on 1,528. The corrections were correct and present. They were skipped because each field had a seeded source column whose mapping was APPROVED, and the overlay treats an approved mapping as the analyst overruling the rule. But approved is not proof a PERSON approved it: the learning engine approves the mappings it applies, under approved_by=learning-engine, and those rows were passing the guard. So a seeded mapping was outranking the analyst's own later correction -- the thing it was written to correct. The guard now requires a human approver, which is exactly the line the analyst drew: a person editing and approving in the UI outranks everything, an engine approval does not outrank the person who wrote the correction. Same test learning_service._eligible already had to make. A row with no recorded approver counts as not-a-person, because reading those as human approvals would restore the bug. Then three columns that were empty for three different reasons, all found by checking the rules against the 258 real headers of the NetSuite extract instead of against themselves. SUPPLIER SITE was blank on all 8,561 rows even though its rule is CONCAT of Country Code and City and BOTH columns exist: the generator prunes the source frame to the columns something claims and builds the per-row dict from the same set, and the overlay's own rules claimed nothing. Every strategy rule deriving from an unmapped column was evaluating against blanks and returning its default. The transformation-rule pipelines had been given this treatment; the overlay was added later and never inherited it, which is how this codebase has lost a guarantee before. The overlay now declares the columns its rules read. PARENT SUPPLIER was blank because SELF_LOOKUP reads its index from ctx self_index and NOTHING IN THE CODEBASE EVER BUILT ONE. It shipped, passed unit tests against a hand-made index, and returned its default on every row of every real run. The index is now built once per source frame before chunking, because a child's parent is usually in another chunk, and as a dict because 7,495 vendors scanned pairwise is 56 million comparisons. Columns match case-insensitively: the analyst wrote Internal Id, the extract says Internal ID, and losing the whole lookup to one letter is not a failure worth having. DELIVERY METHOD was blank because the rule looks for Remittance Email and Remittance Fax and the extract has no Remittance column at all - its headers carry Email, Alt. Email and Fax - and because the 13-Jul strategy's own narrower Supplier Site rule was shadowing the 30-Jul all-sheets one. Both are fixed, and directive_for now resolves ties by DATE. That is the analyst's precedence rule stated in full on 30-Jul: analyst manual change or the mapping file, whichever is latest, then learnings and golden records, then AI. Every rule file carries an effective date and every directive carries it, so a person's approval wins until the analyst issues a newer instruction in a file, at which point the file is the more recent statement of the same intent. A directive with no date loses to any human approval, which is the previous behaviour and the safe default for older files. Finally, a blank correction now CHANGES the library and the mappings rather than being added alongside them: the contradicting column_mapping rows are tombstoned and every existing conversion's mapping row is rewritten to not_applicable with source and default cleared. Seeding a suppression while leaving the old mapping alive left two rows saying opposite things about one field, and the older one kept winning. Mappings a person approved are left alone and counted as skipped_human rather than swallowed. Verified end to end against the real column spellings: Parent Supplier resolves to the parent's name, Supplier Site emits US-Chicago, Delivery Method emits EMAIL then FAX on every sheet. Then I tested the deployed instance through its own API instead of trusting the code, and it found three more. One: the blank enforcement was matching NOTHING live. It retired only column_mapping and example_default, while Tax Reporting Name -- declared blank -- still held a column_mapping from a gold example, a rule from a prior conversion AND an example_mapping, all binding it to Legal Name. It now retires every kind except the suppression itself, because listing kinds means the next kind someone adds silently reopens the hole. Two: the sweep was scoped to the correction's own client with an exact match, so the GLOBAL gold-example row survived -- and a global row is precisely what reaches this client, so the correction was recorded and then overruled by the thing it was meant to retire. Both the library sweep and the mapping rewrite now skip a row only when BOTH sides name a client and they differ; an unscoped row is not another tenant's. That is why nothing had been rewritten live: Supplier Name New was still suggested against New Vendor Approval and Tax Reporting Name still approved against Legal Name, on the very conversion the correction was written for. Three, and this one would have silently undone the whole day: a DERIVED field has no source column by nature and its mapping row is routinely not_applicable for exactly that reason -- and the generator skips a discarded column BEFORE the overlay block runs. Delivery Method is not_applicable on the live instance, so every fix made to that rule today would still have produced an empty column, and the same trap was already set for Supplier Site, Parent Supplier and Tax Organization Type. The directive is now resolved before the discard check and a rule or constant keeps the column alive; a BLANK directive still skips, because blank is what discarding means. A seam test asserts the ORDERING, since the ordering is the whole fix, and that only one lookup exists so the two cannot drift apart. Also added a reseed-supplier-corrections endpoint: these ran only at startup, which made every did-it-land question cost a redeploy and a cold boot, and it returns per-field enforcement counts so it-ran and it-changed-something stop looking identical. And the answer to why the tool was STILL following gold learnings: the library had no notion of when an instruction was given. Competing learnings for one field were ordered by TRANSFORM STRENGTH alone, so a gold example captured weeks earlier beat the mapping workbook the analyst had just handed over, and no date written anywhere could change that. Every learning now carries effective_date -- the date the INSTRUCTION was given, which is deliberately not captured_at: every startup seed stamps utcnow, so ordering on captured_at would make the 13-Jul strategy look newer than the 30-Jul corrections after any redeploy and the whole precedence would invert on a restart. Seeded rows take the _effective_date of the file they came from; rows captured from an analyst's own UI action leave it null and fall back to captured_at, which for them IS the moment the instruction was given. Candidates are now sorted latest-first, with transform strength demoted to a same-day tie-break so the Taxpayer ID protection survives. The defaults pass is ordered the same way, where first-come was letting an older gold default hold a field the analyst had since re-specified. That completes the precedence the analyst stated: 1) analyst manual change or the mapping file, whichever is latest 2) learnings and golden records 3) AI. Live-tested the deploy through its own API, and the reseed answered honestly: learnings_retired 0, mappings_blanked 0, nothing but skipped_human. Both zeros had causes invisible from the code. CLIENT SCOPE: the analyst docs seed under the bootstrap client while the gold-example rows carry the client of the project they were captured in, so comparing against the correction's own client id retired NOTHING -- including the column_mapping still binding Tax Reporting Name to Legal Name. The conversion pass now runs FIRST and collects the clients whose conversions the correction actually reached; a scoped learning is retired when its client is one of those, which is the proof it is in scope, and global rows always go because a global row is precisely what reaches this client. Another tenant's row is still never touched. HUMAN APPROVALS WITH NO DATE TEST: every skipped_human was an approval made BEFORE the 30-Jul correction, and a correction dated after someone's approval is that same analyst changing their mind. The gate is now date-aware, matching the generator. Also: the enforcement is BATCHED. Per-field it re-walked all 232 conversions for each of six blanks across six sheet objects -- 36 sweeps, two database round-trips per conversion each -- and the on-demand reseed took minutes. One pass, resolving each conversion's object and client once and caching template fields. Then CW_Issues 2 arrived, and its Sheet4 held the number that mattered: 1,256 of 1,365 Item fields came back 'No confident match found'. It was filed as a scoring weakness -- 'most of them only check type compatibility and keyword check' -- and it was arithmetic. suggest_mappings is called ONCE with every field of every sheet, 1,365 for Item across 17 sheets and about 1,250 for Customer across 19, and it kept a SINGLE used_sources set for the whole call. A source column could be spent on exactly one target in the entire template, so the number of targets that could EVER be mapped was capped at the number of source columns -- a few hundred. The residual is the reported number, near enough exactly. The second-order damage was worse than the count: Oracle REQUIRES the same key on every sheet, and the matcher spent Item Number on the first sheet and left the other sixteen empty. The analyst filed that separately, from the other end, as 'id should be mapped to Party Original System Reference in all sheets, except HZ_IMP_CLASSIFICS_T'. Same defect, two tickets. The used-set is now scoped PER INTERFACE SHEET, which keeps the property the rule exists for -- inside one sheet a column must not be smeared across several unrelated fields -- and drops the one it never intended. TargetField carries sheet_id and _target_fields_for populates it, with a seam test because the scoping is worthless if the sheet is dropped on the way in. A five-sheet fan-out that mapped 3 of 15 targets now maps 15 of 15. Then, from a screenshot: why is Tax Organization Type INDIVIDUAL for so many? Because a BOOLEAN column was being read for PRESENCE. The branch was if_column 'Is Individual', op notblank -- and in the NetSuite extract that column says 'No' on 6,985 of 7,495 rows, 'Yes' on 437, and is empty on 73. 'No' is not blank, so 7,422 suppliers were written as INDIVIDUAL, companies included, and the only rows that came out CORPORATION were the 73 where the column said nothing at all. Exactly backwards, on 99% of the file. Measured before and after against the real extract: CORPORATION went from 73 to 7,058 and INDIVIDUAL from 7,422 to 437. The 437 are exactly the rows whose source says Is Individual = Yes. A first pass had 456, and the extra 19 were a mistake caught by asking the output who they were: those are the rows where Entity Type says Individual while Is Individual says No, and 17 of the 19 are plainly companies -- Air Liquide Brasil LTDA, RYNDACK COMERCIO E IMPORTACAO LTDA, EDIFFICA ENGENHARIA E PROJETOS LTDA. Entity Type is a FALLBACK, not a second opinion, so an explicit No now outranks it; and it could never have filled a genuine gap here anyway, because this extract has ZERO rows with a blank Is Individual and a populated Entity Type. The fallback is kept rather than deleted because eBOS and SyteLine extracts are not this one. New istrue/isfalse operators read the spellings these extracts carry -- yes/y/1/true/t and no/n/0/false/f, trimmed and case-insensitive -- sharing MAP_BOOLEAN's vocabulary so two rules cannot disagree about what Y means. isfalse is deliberately not the negation of istrue: blank and junk are neither, or 'unknown' silently becomes 'false' and the default branch never runs. Worth recording that the 13-Jul strategy quotes the instruction as 'if there is a value in that, only in that case this value will change', which notblank reads faithfully -- the DATA is what shows the words meant 'if it says yes'. A sweep test then found the same trap in the Customer rules: Default Billing and Default Shipping are NetSuite T/F flags read with notblank, so an 'F' counted as set and every address would have become BILL_TO. Eighteen branches switched; Email, Phone, Fax, First Name and Last Name stay on notblank because those ARE presence checks. Finally Supplier Site, which the analyst respecified three times: code-hyphen-city, then keep just the code with no city and just the city with no code, then derive the code FROM the city. Country Code is already clean ISO-2 in this extract -- every non-blank value is exactly two characters -- so the rule joins rather than converts, and picking the 'Country' column next to it would have produced 'United States-Chicago' on 2,592 rows. The derivation learns city-to-country from the extract's own 6,196 complete rows, which beats any bundled table and cannot go stale, and takes the MAJORITY because ambiguity is real: New York appears against US 48 times and CN once. Measured on this extract: 6,196 rows have both, 1,066 have a code and no city, 233 have neither, and ZERO have a city without a code -- so the derivation has nothing to resolve here today, and nobody should read its passing test as evidence it ran. And rule corrections now reach the conversions that ALREADY EXIST, not just new ones and the file: previously the mapping rows of existing conversions kept whatever the matcher had guessed, so the analyst opened one, saw the old derivation on screen, and had no reason to believe the file said anything different. A re-read of CW_Issues 2 then turned up the last genuinely open rows, and they had failed in the way this codebase keeps failing: the mechanism shipped and no data ever used it. LearnedMapping.sheets / exclude_sheets and sheet_allowed had existed for a while and were correct, and NOT ONE seeded learning in the entire catalog used them -- the count was zero. So 'map id to Party Original System Reference in all sheets EXCEPT HZ_IMP_CLASSIFICS_T' was applied to all sheets, the named one included, because nothing ever wrote the exclusion down. Rows 13, 14, 15 and 26 are now seeded as data with their scope attached: id excluding the classifications sheet; entityid feeding BOTH Account Number and Customer Account Source System Reference while excluding pay-method, banks and account-rels; internalid excluding four named sheets; and Receipt Method and Start Date suppressed on RA_CUSTOMER_BANKS_INT_ALL ONLY, because both fields exist on other sheets and an unscoped name-keyed suppression would have blanked them everywhere -- the same 'one approval reaches all 19 sheets' problem pointed the other way. The seeder updates the scope on an existing row rather than inserting a duplicate, since two learnings for one source/target pair differing only in scope is a coin toss. Tests assert through the real sheet_allowed against the seeded data, not the mechanism in isolation, because testing the mechanism is exactly what let it sit inert. The analyst then sent the regenerated bundle, and measuring it found three more -- none of which any unit test was looking for. The bundle was right about almost everything: Batch ID empty on all six sheets, Supplier Name New empty, Procurement BU and Liability Distribution empty, Business Relationship SPEND_AUTHORIZED, Parent Supplier resolving to real names, Delivery Method populated on 3,686 of 3,872. But ALTERNATE NAME WAS BLANK ON EVERY ROW, 0 of 3,872. The correction carried a CASE_WHEN that returned empty on its only branch AND on its default, so it could never do anything except blank; it had been harmless only while the overlay was not reaching that field, and the moment the overlay started running it destroyed all 1,286 legitimate alternate names. The real rule is BLANK_IF_EQUALS, which compares the two finished OUTPUT columns -- a row-local CASE_WHEN cannot express this at all, because the per-row context holds SOURCE columns while 'Supplier Name' is a TARGET field. The dead CASE_WHEN is removed, and a test now fails any overlay CASE_WHEN whose every path returns blank, because a rule that cannot produce a value is a blanking rule wearing a disguise. INACTIVE DATE carried the literal 'No' on all 3,813 contact rows -- a Yes/No value in a DATE column, which Oracle rejects -- because the correction was filed against Supplier alone while the Supplier, Address and Site sheets were correctly empty; it is now all-sheets. And THE SITE KEY DISAGREED WITH ITSELF: POZ_SUPPLIER_SITES_INT said 'Hyderabad' where POZ_SITE_ASSIGNMENTS_INT said 'BU Hyderabad' for the same site, so 3,878 assignment rows could not have matched a single site. The site-key rule is now all-sheets across Supplier Site and Supplier Site Assignment. The banks sheet also carries a Supplier Site column, holding numeric references rather than names, and is DELIBERATELY left alone -- whether those become the country-city key is a business question, and that omission is asserted in a test so it stays a decision on the record rather than looking like an oversight later. And the rest of the site-key defect: consistent across sheets is not the same as CORRECT. The 30-Jul bundle shipped 'Hyderabad' on 4,388 rows where it should have said 'IN-Hyderabad' -- city present, country code empty -- because a sheet is routed to whichever bound source supplies most of its mapped columns, and that frame does not carry the plain 'Country Code' the rule named. It holds the Billing and Shipping spellings. Both the country and the city are now CANDIDATE LISTS, first non-blank wins, matched case- and punctuation-insensitively, with the ISO-2 code columns leading and the full-name 'Country' column allowed only as a last resort -- because 'Country' holds 'United States' and choosing it would have produced 'United States-Chicago' on 2,592 rows. Every candidate is declared to the generator, since a column the rule might read but never declared is a column it will never see, which is how Supplier Site shipped empty on 8,561 rows to begin with. Verified across all six shapes: plain, billing-only, shipping-only, mixed, city-only-with-the-code-derived, and code-only. Analyst, on the 427 capitalisation collisions: 'Keep it IN-Hyderabad for now.' The site key is REQUIRED and UNIQUE and the extract shouts at random -- Hyderabad 461 times against HYDERABAD 103, Dubai 25 against DUBAI 3 -- so Fusion would have created two sites wherever one exists. Measured: 429 keys collided with another key on capitalisation alone; now 0, and IN-Hyderabad carries all 565 of its rows. The canonical spelling is chosen FROM the data rather than by title-casing, because str.title() produces 'Rio De Janeiro', 'Ciudad De Mexico' and 'Mcallen' -- it breaks eight of the Spanish and Portuguese names in this extract alone. A NON-ALL-CAPS spelling wins outright, ahead of frequency, and only then does the more common form win: ABU DHABI appears four times against Abu Dhabi once, and frequency alone would have kept the shouting. It only ever picks a spelling the file actually contains, so no place name is invented, and 219 cities that appear ONLY in capitals are left exactly as they are -- there is nothing to correct them from, and they have no collision to fix. Then the Customer Import sequence workbook arrived, and it documents a trap worth naming: Oracle's Customer Import WORKSHEET column order and the order of the CSVs its macro generates ARE NOT THE SAME. Twelve of the fifteen interfaces agree. Three do not. HZ_IMP_ACCTSITES_T moves Account Number from position 3 to 46 and Party Site Number from 8 to 47; HZ_IMP_ACCTSITEUSES_T moves the same pair from 1 and 2 to the final two slots of 45; RA_CUSTOMER_PROFILES_INT_ALL moves six columns -- Party Original System and its Reference, Credit Analyst, Credit Review Cycle, Last Review Date and Next Review Date -- past position 100. This is the worst shape a defect can take here, because the column COUNT is identical: a file built in worksheet order is indistinguishable from a correct one by eye, and the CSVs are HEADERLESS, so POSITION is the only thing carrying meaning. Ship HZ_IMP_ACCTSITEUSES_T in worksheet order and Oracle reads every Account Number as an Account Site Source System, on every row, and raises nothing. The order is now data, extracted from the workbook and covered by tests that pin the measured indices rather than a rule of thumb -- a first draft asserted 'lands in the last five' and failed, because on the larger interface the pair lands after the thirty flexfield segments but BEFORE the Global Attribute block. There is no tidy rule, which is the argument for encoding it. Generation now writes the Customer bundle with the CSV order, Oracle's own file names (HzImpPartiesT, HzImpAcctSiteUsesT, RaCustomerProfilesIntAll and the rest -- Oracle matches by name, so a correctly ordered file under the wrong name is simply not read), and in LOAD SEQUENCE, numbered so the order survives an analyst unzipping into a folder. Parents before children, because Oracle rejects a child row whose parent has not loaded. The xlsx keeps the worksheet order, since that is what a human opens. Implemented in the existing supplier layout module rather than a parallel customer one -- a second copy of a rule is how this codebase has lost guarantees all week. Then the HCM (Employee) field-mapping workbook, where the instruction was 'only the mappings that are GREEN'. Green is the workbook's own legend for Mapped, against Yellow for questions to NextPower, Orange duplicate, Blue Oracle-required-but-missing and Red not-to-bring, and it is read by CELL FILL on the Source Column Name cell rather than by eye -- 20 of 47 rows, every one of which also reads Bring to Oracle = Yes. Four shapes in that sheet are not a plain column mapping. The mapping tab writes source columns with UNDERSCORES, Employee_ID and Legal_First_Name, while the actual Workday extract header row uses SPACES; the apply path normalises so nothing would have failed a test, but propagation writes original_value VERBATIM onto the mapping row, so the workbook spelling would have put a source column on the analyst's screen that the dataset does not contain. One source feeds TWO targets -- Country goes to both Country and LegislationCode, and they land in different .dat files, so one learning could not have served both. A CONSTANT sits where a source column goes, 'default value ( 1/1/1900 )', which seeded as a column would send the matcher hunting the extract for a column of that name. And the (Object) suffix is a SCOPE, not a label: EffectiveStartDate exists on NINE components, so EffectiveStartDate(Location) means those two rows and no others, or every worker's record gets date-stamped 1900. Then the failure this project keeps repeating caught the first cut of this work, and it is worth recording that it was caught by cross-checking against hdl_schema rather than by any test of the new file against itself: for HDL, a 'sheet' in this tool is a COMPONENT name -- hdl_seed_service writes one FBDISheet per component -- and I had scoped every learning to the top-level .dat object. FirstName is on PersonName, EmailAddress on PersonEmail, LegalEmployerName on WorkRelationship, WorkTerms and Assignment; none of them on a sheet named Worker. Eleven of nineteen learnings were scoped to the one sheet their field is not on, which excludes them from the only sheet they were written for and leaves the list looking complete. The scope is now measured from hdl_schema.all_components(), and a seam test fails if any seeded field is not actually carried by the component it claims. Three loader changes the workbook mandates, none of which the JSON alone would have delivered: Location and Job EffectiveStartDate become the constant 1900/01/01, which matters because both are deduped SETUP objects and the hire date they carried came from whichever employee row happened to survive the dedupe -- arbitrary, and a different date on the next extract. PersonName gains KnownAs from Preferred Name, because it had no such attribute and the green row had nowhere to land. And OnMilitaryServiceFlag stops being hard-blank and reads Military_Service: the strategy doc had said 'pending Business confirmation' and blank was honest then, but the workbook names the source, and blank would now DISCARD the value the moment that Workday file joins the load -- it still renders empty today, from an absent column rather than from a decision. Cost Center is green and has nowhere to go: the workbook's own comment is 'No suitable field is available in Oracle for this information', so it is recorded for the analyst and deliberately NOT seeded as a live mapping, because a learning pointing at a phantom field reads as done and does nothing. Five of the twenty green rows name columns this extract does not contain -- Job Profile, Job_Code, Location_Code, Manager_ID and Military_Service, all with a blank Source File cell because they come from other Workday files -- and they are flagged rather than assumed. Everything lands in the Learning Center as ordinary learnings, same collection and same list, scoped to source system Workday and object Employee HDL, seeded at startup and re-seedable on demand via reseed-hcm-mapping. Then the 31-Jul issue list, which reads as nine separate Customer bugs and is the analyst saying one sentence nine times: I set this in the UI, the screen agrees, the mapping sheet agrees, the FBDI file does not. Four causes, none of them where the symptom pointed. ONE, the generated linkage overwrote the person. customer_structure_service fills what no source system supplies -- Batch Identifier, Party Original System, Customer Account Source System, Account Site Source System -- and it wrote ALL of them unconditionally. Glue is a fallback for what the analyst has not filled in; it was behaving as an override, which is why Keep blank on Batch Identifier came back and why three NETSUITE defaults never appeared. The sharpest case is Account Site Source System, which did not merely fail to write the analyst's value -- it wrote EMPTY STRING over it, because apply_to_frame was called with a hardcoded level='account' for every sheet and the account branch blanks the site key. On HZ_IMP_ACCTSITES_T and HZ_IMP_ACCTSITEUSES_T the site key is the entire point of the sheet. Columns the analyst has decided are now off limits to the glue, matched loosely so a '*Batch Identifier' header is protected by a 'Batch Identifier' decision, and the level is derived from the sheet -- for the two sheets whose level is not in doubt and no others, because inventing a party-level sentinel on sheets nobody has reported would be changing behaviour on a guess. TWO, a default written onto zero rows. An optional interface emits data only when a real source column is mapped into it, which is right -- a naive fan-out once put 5,599 rows of pure glue into all 19 sheets -- but 'content' was defined as A SOURCE COLUMN, so a sheet whose entire contribution is constants was written headers-only. That is Role Type reaching HZ_IMP_ACCTCONTACTS_T, which has mapped columns, and not HZ_IMP_CONTACTROLES, which does not; and Relationship Type and Relationship Code reaching neither HZ_IMP_RELSHIPS_T nor HZ_IMP_ACCOUNTRELS. An analyst-typed constant now counts as content. Person-set only, deliberately: an engine-seeded default must not be able to switch an optional interface back on, or the problem the suppression exists to prevent returns through the back door. THREE, the frame is keyed by FIELD NAME, so a per-sheet decision could not be expressed at all. Both halves were reported the same day from opposite ends -- Role Type is one sheet too few, Insert Update Indicator ('should only reflect in the RA_CUSTOMER_PROFILES_INT_ALL sheet') is one sheet too many -- and they are one mechanism. MappingSuggestion rows are already per-sheet, so the decision existed and only the frame could not carry it; each sheet's own rows are now re-applied to that sheet's finished frame, after the control defaults, because the analyst outranks them. A default alongside a mapped column fills BLANKS only, since overwriting a real mapped value with a fallback constant is the eBOS 'Address Name shows city in the UI, ships PRIMARY' bug. And captured defaults now record the sheet they were set on: LearnedMapping.sheets and sheet_allowed existed, defaults_service never consulted them, so the scope was recorded and ignored -- this codebase's signature failure, for the third time. FOUR, and this is the one that would have shipped a broken file that looked perfect: 'Cannot apply transformation logic where the value of a target field (Party Number) depends on the value of another target field (Party Type).' Four things were wrong at once. The analyst's own TransformationRule rows were read AFTER the discard guard, so a rule they authored never ran on a field whose mapping is not_applicable -- which is exactly what a derived field's mapping IS. The strategy overlay was rescued from that trap on 30-Jul and the rules analysts type were left in it; two rows of the issue list say 'Tried using custom transformation rule, but its not working' in the status column. The per-row context is built from SOURCE columns, so a condition naming a TARGET field looked it up, got None on every row, and fell through to its default -- the same shape as BLANK_IF_EQUALS, which had to be lifted out of the row-local engine for the same reason. The context now falls back to the target columns computed so far, source first so every rule that resolves today resolves identically and only the lookups that used to find nothing change; fields are computed in target-sequence order, and Oracle puts Party Type at column 4 and Party Number at column 5. row_index was NEVER PASSED during generation -- only the mapping preview endpoint set it -- so SEQUENCE read ctx.get('row_index', 0) and returned start+0 for every row: Party Number, a required UNIQUE key, would have shipped NXT000001 on all 5,489 rows while the preview showed a correct running sequence. The screen being right is what makes that one dangerous. And it had to be the GLOBAL row number, not the chunk-local one, or the numbering restarts every 20,000 rows and the eighteen sheets that reference Party Number stop agreeing with it. Verified end to end: Party Type derives PERSON/ORGANIZATION, Party Number runs NXT00001_C1, NXT000002, NXT00003_C1, NXT000004 and continues correctly from a chunk offset, and a real source key still beats a manufactured one. Alongside those, V2_2 of the Customer sequence workbook. Re-extracting it and diffing against V2_1 gave column-for-column agreement on all fifteen interfaces and the same three reorderings, so the second workbook confirms the first rather than correcting it -- worth recording, because if it had disagreed the shipped CSVs would have been silently wrong for two days. Its real additions are two. Every one of the 15 CSV columns now ends with an explicit END row: the supplier package has always written the record terminator and the customer package never did, so Customer CSVs shipped without one. And the Summary tab now names the CSV files, which match the names already derived. Scope, from Tejaswini the same morning: 'they are working on 15 files only, mentioned in the sheet, so we do not have to generate all of the 19 FBDI output files.' The four dropped -- HZ_IMP_ACCOUNTRELS, HZ_IMP_CLASSIFICS_T, RA_CUST_PAY_METHOD_INT_ALL, RA_CUSTOMER_BANKS_INT_ALL -- are the SAME four the analyst had been naming one at a time as per-field exclusions ('in all sheets except HZ_IMP_CLASSIFICS_T', '...except RA_CUST_PAY_METHOD_INT_ALL, RA_CUSTOMER_BANKS_INT_ALL, HZ_IMP_ACCOUNTRELS'). Those exclusions were an analyst working around a file set that was too big; stated once as scope they stop having to be restated per field. It is DATA, so putting an interface back is a one-line edit, which matters because the same issue list asks about a default on HZ_IMP_ACCOUNTRELS -- and the excluded set is logged on every generate, because a deliverable that quietly got smaller is worse than one that says so. An interface the spec has never heard of stays IN: scope narrows a deliverable, so its failure mode has to be inclusion. Finally, editing the DEFAULT of an already-approved mapping now restamps approved_at. exclude_unset means status is absent on that call, so the stamps stayed at the original approval and an edit made today could lose to a rule file dated yesterday -- the generator's date test was reading a timestamp that no longer described when the decision was made. Then the three documents behind CW #28 -- 'some fields are not being reflected as expected. For example, the PersonName, AssignmentSupervisor' -- arrived, and the right move was to stop reading notes about the data and read the client's own HDL template, which is a hand-built load file with a real sample row for employee 1007802. Generating that employee from their real input and diffing attribute-by-attribute against their row is the client stating what they expect to load, and it found seven things no test was looking for. FIRST, THE INPUT IS NOT THE EXTRACT. The tool is fed INTXXX_CR_Oracle_Fusion_Demographic_V2 -- 23 columns, header on row 2, spelled with UNDERSCORES -- not the 31-column 'Workday Extract' tab of the mapping workbook. All five columns I had flagged as 'from other Workday files' (Manager_ID, Location_Code, Job_Code, Job Profile, Military_Service) are in it. Column matching normalises punctuation, so underscore-vs-space was never the problem; a DIFFERENT column was. JobCode's canonical source was 'Business Title' and the real file carries a proper Job Code holding GSSSSM_VP2 -- the exact value the client's template shows -- so the tool was writing 'Sr. VP, Supply Chain Management' into a job CODE column. Sources are now candidate LISTS, first match wins, which costs nothing when the first spelling is present and is the whole difference when it is not. PersonName was MISSING MiddleNames, with KnownAs sitting in its slot: that IS 'the PersonName fields are not being reflected', and getting a component's attribute list wrong is not cosmetic, because HDL reads METADATA positionally against the names on that line. PersonEmail was missing PersonId(SourceSystemId), the link back to the Worker record -- without it the e-mail has no person to attach to. Assignment carried DepartmentName where the template carries DefaultExpenseAccount, and that settles the mapping workbook's Cost_Center row, which I had written off as having no Oracle field on the strength of its own TRX comment ('No suitable field is available in Oracle for this information'). It does exist. Deferring to a note ABOUT the data instead of reading the artefact is the mistake, and it is worse than a wrong mapping because a row marked 'no field' is never looked at again. It is populated from Cost_Center now and flagged: the template shows a full accounting flexfield (1200.00000.10000.0000.0000.000.000000) where the column holds one segment (200), so a crosswalk is still owed -- populated-and-visibly-short beats absent-and-invisible. Assignment's JobCode and LocationCode shipped blank and the template populates both. Location's LocationCode was copying the location NAME instead of Location_Code (NX_USA_CA_08). WorkTerms' BusinessUnitShortCode was blank as open item OI-02; the template answers it, NXT LLC BU, and a required attribute shipped empty stops being the safe option once the answer is written down in the client's own artefact. Position uses SourceSystem / SourceSystemID, NOT the SourceSystemOwner / SourceSystemId every other component uses -- HDL matches attributes by name, so that near-miss is ignored in silence and the position loads with no source key at all. And the sixth output file, which the analyst named: 'Worker has two -- Worker(Employee) and Worker(Assignment Supervisor)'. It is a two-PASS load and the reason is ordering: a supervisor row points at the MANAGER's assignment number, so every worker, managers included, has to exist before any of those links can resolve. Pass one leaves AssignmentNumber and AssignmentStatusTypeCode to Oracle; pass two re-MERGEs WorkRelationship, WorkTerms and Assignment with E<employee id> and ACTIVE_PROCESS set, then adds the AssignmentSupervisor block. Before this, AssignmentSupervisor emitted E1007802 while every assignment row carried a blank assignment number -- a reference to something that did not exist, which is the other half of CW #28. Both passes write Worker.dat because HDL identifies the object by file name; they are told apart by the zip, which now carries the template's own sheet label. The two WorkTerms and Assignment variants are built from one definition so they cannot drift, and all_components() dedupes by component name so the shared components do not become two FBDISheets called 'WorkTerms' and split every mapping and per-sheet scope across a pair of sheets that are the same thing. Also: the setup EffectiveStartDate constant is 1990/01/01, not 1900/01/01 -- the mapping workbook writes '1/1/1900' and the template says 1990-01-01, one digit apart, and the template is the artefact they load. OnMilitaryServiceFlag defaults to N where the extract is silent, per the template, and still reads the column where it is populated (23 of 2,773 rows). Verified: every attribute of every component now matches the template's list and ORDER, and employee 1007802 renders identically to the client's hand-written row apart from two knowing differences -- the company name, where the template uses an anonymised 'Nextpower LLC' against the input's real 'Nextracker LLC, USA', and DefaultExpenseAccount pending that crosswalk. Then the 27-Jul list, four defects, three of them invisible by construction. TWO SHEETS, TWO CONVERSIONS. A Customer+Address workbook is fanned out to one row per sheet, and the create-all loop called generate-set once PER ROW -- two conversion sets for one workbook, each bound to one sheet. The per-row button on the SAME PAGE had always collected the siblings and sent dataset_ids, which is the tell: one page, two behaviours, and the primary button was the wrong one. Rows are now grouped by the underlying File. The second half is worse than an omission: the source-columns endpoint read the singular conv.dataset_id while the auto-mapper unions source_dataset_ids, so the AI could propose a mapping to an Address column the analyst could then neither see nor re-pick from the dropdown -- two layers disagreeing about what the SOURCES are. It now queries every bound dataset and collapses repeated column names, since both sheets carry entityid and a mapping stores a name. The fanout's idempotency was keyed on the PRIMARY dataset alone, so a second call naming a different sheet collided with nothing; it now also matches on overlapping sources, deliberately not on template alone, because a project may legitimately load two different Customer extracts. THE TOMBSTONE FAMILY, FOURTH APPEARANCE, and this time the audit was the problem. LearnedMapping.find injects is_deleted $ne true, so a retired row is invisible to a plain find_one -- the code sees nothing, inserts, and the deleted learning is back beside its own tombstone. SIX write paths had no idea tombstones existed: manual-map save, apply-proposal, accept-value-map, mapping-workbook import, prompt steering and classify-learn. The audit that was supposed to catch this COULD NOT: it flagged functions that MENTION is_deleted while querying without include_deleted -- a DEAD guard -- and none of the six ever mentioned it. A test that can only find the bug in code that already knows about the bug is not a test. It is now anchored on the INSERT, which is the thing that does the damage: any function constructing a LearnedMapping must have read with include_deleted=True somewhere in its own body. Verified by planting an unguarded writer and watching it fail. Beanie's Document.get() delegates to find_one, so get() is tombstone-blind too and takes no include_deleted at all -- including in the delete endpoint itself, where it meant an already-retired row could never be purged: the only rows you could purge were the ones you had not deleted yet. get() is now banned on this model, by test. The revive rule follows the line the original fix drew: classify-learn runs BY ITSELF on upload so it respects a tombstone and reports that it did; typing a mapping, accepting a value map, importing a workbook and writing a prompt are the analyst acting, so they revive IN PLACE -- one row, not a ghost pair. And clearing a manual mapping now tombstones instead of hard-deleting, because a hard delete leaves nothing for the next startup seed to respect and the seeds run on EVERY boot. DEFAULTS MISSING FROM THE LEARNING CENTRE was a scope filter, not a capture failure. client_id_for_conversion can return None; the capture then writes client_id=None with is_global=False, and the Learning Center's hand-rolled filter matched NEITHER is_global nor client_id -- so the row existed, applied to the file, and could not be seen on screen. scope_query already rescues exactly those rows for the defaults layer; this endpoint did not use it. Two readers of one collection disagreeing about what is in scope. Alongside it, the object key: a learning is written under the template's business_object and read by target_object, and three bundled templates carry business_object 'Supplier' while their conversions are 'Supplier Import' / 'Supplier Address' -- so the row landed in a different accordion group and the tab the analyst was looking at showed nothing. Where the two spellings disagree the reader's wins. ADDRESS NAME SHIPPING PRIMARY: 'address name': 'PRIMARY' was still in _CONTROL_DEFAULTS. Removing it from _AUTHORITATIVE was not enough, because the plain control-default branch fills any column that reaches finalize entirely blank and has NO explicitly_mapped guard at all -- so whenever the city failed to materialise in the frame the constant went onto every row, and the screen (which reads the mapping) and the file (which reads the frame) disagreed exactly as reported. The signed strategy says the opposite in so many words: 'Address Name is the City Name (e.g. Austin). NOT the constant PRIMARY.' A control default is for a column nobody has an opinion about; this one has a signed opinion, so an unpopulated Address Name now ships EMPTY and is visible as a gap. THE RULE DIALOG was crashing, silently. RULE_SPECS in the modal defines 24 rule types and the backend accepts 32; the missing eight -- PHONE_PART, PREFIX, SELF_LOOKUP, SEQUENCE, SUFFIX_WHEN, MAP_BOOLEAN, CONDITIONAL_DATE, SUFFIX -- are exactly the ones the SUPPLIER catalog seeds. Reopening the dialog on such a field looked up RULE_SPECS[type], got undefined, dereferenced it on the next line, and React unmounted the tree with no error boundary anywhere in the app. The analyst saw an empty dialog and reported 'the saved transformation does not appear' for a rule that saves correctly and reaches the output file. Every backend type now has an entry, editable through the raw JSON editor -- a form we have not written is a reason to SHOW the config, not to lose it -- plus an unknown-type fallback so a rule saved by a newer build cannot take the dialog down. And the bare catch{} around the rules fetch is gone: a failed list was indistinguishable from 'no rule saved', which is precisely how the earlier ObjectId 500 presented, so it now warns. CONFIDENCE BANDS are the analyst's, not ours. They asked for 90-100, 80-90, 75-80, 50-75, below 50, 0; the export shipped 95-100, 90-95, 85-90, 75-85, 50-75, 0-50 -- the same idea at different cut points, close enough to look done and impossible for the functional team to reconcile against what they asked for. Now theirs exactly, with every boundary pinned on both sides and a test that every value 0-100 lands in exactly one band, since a gap would drop fields out of the workbook silently. Also, on the HCM work: the client's template is the FORMAT the download must take and the mapping file is the authority for which column feeds which field -- the analyst drew that line explicitly, so the conformance test asserts structure (every attribute present, in the template's order, extras allowed) and no longer pins the template's sample VALUES, which are one hand-written row and partly illustrative. Cost_Center maps to DefaultExpenseAccount as the mapping file writes it, with no crosswalk invented. Address Name was then reported a third time, with the rule dialog open in the screenshot, and the screenshot showed something the earlier fix would not have cured on its own: the analyst had saved a Value-mapping rule reading "Address Name <- City", and Mapping Review still displayed SOURCE (none) and "Required field unmapped" while the file shipped PRIMARY on every row. THE RULE'S OWN SOURCE COLUMN WAS NEVER READ. The dialog asks for it -- "the legacy column this rule transforms" -- the API stores it on TransformationRule.source_column, the saved-rules banner shows it back, and the generator dropped it when building the pipeline dict and fed the rule the MAPPING's source value instead. With no mapped source that is an empty string, so the value map evaluated "" on every row and returned its default. Shipped, visible in the UI, and inert -- and the control default then wrote PRIMARY over the empty result, which is why removing the control default alone would have turned a wrong column into an empty one. The rule's column is now used when the mapping has none (the mapping still wins where it has one, because a person binding the field in Mapping Review is the more specific statement), matched case- and punctuation-insensitively, and DECLARED to the frame so it survives pruning -- undeclared, it would have been dropped before the rule ran, which is the fault that shipped Supplier Site empty on 8,561 rows one layer over. And saving a rule now writes the binding onto the mapping: two artefacts describing one decision is the actual problem, so the rule records it where the screen, the required-field gate and the generator all already look. Conservative on purpose -- it FILLS an empty binding and never retargets one the analyst set, and it lifts a not_applicable row, because attaching a rule to a field is saying it should carry a value. Verified against the screenshot's exact state: mapping with no source, VALUE_MAP on City with the dialog's placeholder pairs and a blank default, and the column now carries Hayward, Dallas, SP, New York. Then the analyst stated the governing rule in one sentence -- "the mapping file will provide the initial mapping, the gold standard the initial reference for output, but after conversion if the user wants to change any mapping, remove etc, the tool should apply it and update or include the learning center; THE LAST MAPPING WITH RESPECT TO DATE SHOULD BE CONSIDERED FINAL, and existing and new conversions should map and generate output according to that" -- and six places contradicted it. CAPTURE: PUT /mappings/{id} learned only when the row was ALREADY approved or overridden, and status is absent from the payload unless the UI sends it, so changing the source column of a still-suggested row and saving captured NOTHING. The conversion in front of the analyst was right, the library never heard, nothing propagated -- while the sibling Approve endpoint had no such gate, so one screen had two behaviours and Save was the broken one. A decision is now defined by WHAT changed (source column, default, status, transform), not by the status it happens to be in, and every one re-stamps approved_at, because "last by date" only works if each decision carries the date it was actually made. FORKING: _upsert keyed on rule_type and then matched on original_value, so changing a source column or adding a transform matched nothing and INSERTED a second learning while the first stayed live and competing. "I updated the mapping" reliably produced two rules and a coin toss. One answer per field is a PRECONDITION for last-one-wins -- two live rows cannot express it -- so column_mapping, example_default, suppress_field and reference_standard are now found by (object, field, client, source) alone and updated in place, source column and transform included. Crosswalks are deliberately excluded: those are one row per source VALUE and collapsing them would destroy the map. PROPAGATION: existing conversions skipped EVERY mapping a person had approved, forever, on the reasoning that a library entry must not overwrite a colleague's decision. In a conversion already worked through -- most of them -- a correction therefore reached almost nothing, and "apply to all existing conversions" was never what happened. The rule is not "never overwrite a person"; it is "the later decision wins, whoever made it". The comparison is now a date in both directions: a person's word stands while it is the later one and is superseded when it is not, an engine mapping never outranks anything, and an UNDATED decision counts as older because it cannot be shown to have come later. The number overruled is returned rather than hidden. REMOVAL: Keep blank captured its suppression and stopped, so a field the analyst blanked stayed populated everywhere else -- the one direction where silence is destructive, because a blank plainly means blank. It propagates now, and suppress_field finally has a branch in the patch: it had NONE, so where it did arrive it set status=approved and left the source column in place, doing the opposite of what it says. THE KEY: a learning is WRITTEN under the template's business_object and was READ under the conversion's target_object with exact string equality, so wherever those spellings differ the row is filed where nobody looks -- and because the generator uses the write key, the value reached the FBDI file while being invisible in the Learning Centre and the defaults preview. A correct fix looking broken is worse than a wrong one. Every reader now asks for every spelling the object answers to. THE ORDER: three layers, three orderings. The engine ranked by effective date; compute_effective_defaults had NO sort at all, so the last row in Mongo natural order won -- and since _upsert updates in place, an edited (newer) learning keeps its original early position and loses to any later-inserted competitor, which is a concrete "I updated the default and the screen still shows the old one"; and the Learning Centre sorted by captured_at, which every startup seed re-stamps, so a seeded row with an ancient effective_date sorted to the top of the list while ranking last in the engine. All three now order by the date the instruction was given. And editing a learning IN the Learning Centre reached no conversion at all -- PATCH was a bare set(), POST an insert -- so the library and the files drifted apart in silence and the only way to find out was to regenerate and read the output. Both re-apply now, with skip_origin=False because an edit made in the library has no conversion that is "already correct". Two more things propagation was getting wrong on the way: it wrote the learning's source column VERBATIM with no check that the target conversion's extract has such a column (reads as mapped on screen, produces nothing in the file), and it ignored the sheet scope that record_learning_from_mapping deliberately writes, so it was strictly wider than what the readers would then honour. Both fixed, with unknown-means-do-not-filter on the column check so a missing profile cannot silently narrow the fan-out. The transformation rule still not reflecting in the mapping section had a SECOND half, and it is in the browser: onSaved closed the modal and flashed a message without refetching anything, so the grid and the inspector kept showing their in-memory copy -- SOURCE (none), "Required field with no source and no default" -- for a field the rule had just bound to a column. The server-side sync makes the binding true; the reload makes the screen show it, and fixing only the backend would still have looked broken. Also added docs/CODEBASE_GUIDE.md: architecture, the one flow that matters, the precedence model, and change recipes for the things that actually get touched -- add a rule type, change a default, add a seeded learning, add a UI field, an endpoint, a DB field, an FBDI column order, a new target object -- plus a Traps section where every entry is a bug that shipped: shipped-and-inert, the tombstone, two layers disagreeing, the control-default table, the preview lying, silent swallowing, and field names repeating across sheets. And the analyst immediately found the case that broke the first cut of the rule-to-mapping sync. It only filled an EMPTY binding, on the reasoning that retargeting an existing mapping would be presumptuous -- and Supplier Name showed why that is wrong: the binding it refused to touch had been put there BY THE LEARNING ENGINE ("Auto-applied from learning library"), not by a person, so a rule the analyst had just saved reading `Legal Name` lost to an engine mapping reading `Name`. That inverts the entire precedence. The rule save is the LATEST decision, so it wins; and it is safe because the dialog PRE-FILLS the source column from the mapping, so leaving it alone writes back what was already there and the binding can only move when the analyst deliberately picks a different column. The previous value comes back in the response and the dialog says what moved -- a retarget the analyst cannot see is the same class of problem as one that never happened -- and the outputs built on the old binding are marked stale. Worth noting for the next person: response_model SILENTLY STRIPS keys it does not declare, so returning mapping_sync from the router put it on the wire and FastAPI threw it away until the schema declared it. Then the two ENGLISH boxes, and the steer one was destructive rather than merely useless. The regexes ran BEFORE the model and their order put DEFAULT ahead of MAP, so "supplier name should be mapped to legal name for all conversions" matched `(?P<f>.+?) should be (?P<v>.+?)` and was read as: default the field to the CONSTANT "mapped to legal name for all conversions" -- that whole sentence, written into the column on every row. A fallback that is wrong in a destructive direction is worse than no fallback. The model now parses FIRST and the deterministic parser only sees what it left, which is what the analyst asked for ("all should be done using AI, or a Python function whichever is available"); and the regexes are fixed too, with MAP tried before DEFAULT and explicit patterns for "mapped to/from/with/using". The model is also handed the REAL SOURCE COLUMN LIST -- before, it only saw target fields and had to guess the spelling of the column meant, and a guessed column binds to nothing. Both parsers now resolve names against the actual profile and REPORT a miss instead of writing it, because a source column that resolves to nothing reads as mapped on screen and produces an empty column in the file. Trailing scope phrases are stripped: "legal name for all conversions" is the column "legal name". Two more things the steer path was missing: it never propagated to conversions that ALREADY EXIST -- the half the analyst named -- and it wrote its mappings with no approver and no date, so under "the last decision by date is final" they could never be ranked against anything. Both fixed, with counts returned. The OTHER English box, the translator in the rule dialog, returned rule_type and config and NOTHING about the source column, so "Supplier Name should be mapped from Legal Name" set the target field correctly and left the source picker on the pre-filled `Name`. It now extracts the column the sentence names, prefers the SPECIFIC one where several match ("Legal Name" and "Name" both appear in that sentence and the longer is meant), asks the model for it as well, and says what it moved. It deliberately suggests NOTHING for a pure transform instruction -- "trim the supplier name" is about the column already chosen, and reading a column name out of it would silently re-bind the field. And it read only the FIRST bound dataset, so on a multi-sheet workbook a column on any other sheet could not be resolved at all. Then the analyst drew the line between the two English boxes: the one in the rule dialog is "prompts for better mapping a SINGLE COLUMN", the steer box is "prompts to correct mapping for ALL THE OUTPUT". Both were answering the wrong question. THE SINGLE-COLUMN BOX forced every sentence into a TRANSFORMATION RULE, so "Supplier Name should be mapped from Legal Name" came back as a Value mapping pre-filled with the placeholder pairs active->production -- neither what was asked for nor anything anyone would keep. Three of the four things an analyst types about one column are MAPPING changes and need no rule at all, so intent is detected first: remap points the column at the named source, default writes a constant, blank goes through keep-blank, and only a genuine transformation verb (trim, uppercase, pad, if/when, date, concat...) falls through to the rule parser -- which matters, because "set it to uppercase" must not be read as the constant "uppercase". Save applies the mapping change directly instead of leaving a junk CONSTANT rule in the Rule Library, and for a blank, a rule that would fight the suppression. THE STEER BOX was scoped to ONE business object, and a Supplier load is SIX conversions -- Import, Address, Site, Site Assignment, Contacts, Banks -- each a different object, so an object-scoped fan-out reached one sixth of what the analyst calls the output. It now passes every object in the project's load sequence to the propagation, which widens only the OBJECT match: the field-name match, the source-column-exists check, the sheet scope and the date test all still apply per conversion, so a sheet without that field or without that column is skipped exactly as before. 693 tests over 58 files, run on the pinned pandas 2.2.3 and on 3.0.2.
