# Throwaway parser test files staged during live verification (not part of the app)
_parser_probe/

# Python bytecode (can be generated by tooling that touches the working tree)
__pycache__/
*.pyc

# Doc-generation scratch (node deps + one-off build scripts + render byproducts)
docs/_*
docs/node_modules/
docs/*.pdf
node_modules/

# Deploy inputs - see launch_git.bat. Not source.
*.patch
*.patch_old
COMMIT_MSG.txt
