@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Seed the Mapping Knowledge Base from the metadata catalog: bundle 131 standard source->Oracle FBDI column mappings (NetSuite / Infor SyteLine / Salesforce) as backend/app/data/mapping_catalog.json and upsert them into LearnedMapping (kind=column_mapping, captured_from='metadata catalog', tagged with source_erp) on startup - idempotent + non-destructive. New GET /learned-mappings/catalog-status + POST /reseed-catalog; Learning Center shows a Mapping Knowledge Base panel. Future matching source files auto-apply these."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
