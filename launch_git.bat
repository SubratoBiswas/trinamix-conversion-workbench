@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Convert-a-file (File upload) path: add the 'Conversions from your files (N)' fan-out section - a file detected as Supplier now previews all 6 FBDI objects (Import - Address - Site - Site Assignment - Contacts - Banks) in load order, selected by default with per-step toggles, matching the setup wizard Scope step. Deselected steps are dropped after generate-set on create."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
