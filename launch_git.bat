@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "AI value crosswalks (feature 1/4). Implement the missing value_mapping_service (recommend_value_map / accept_value_map / resolve_value / lov_coverage / _norm) that mapping.py and rule_based.py import - the value-map endpoints were 500ing. Normalizes legacy source values to Oracle Fusion codes: learned crosswalks -> deterministic LOV resolution (exact/meaning/fuzzy) -> Claude Sonnet for the rest (knows country/currency/UOM/status codes even with no stored LOV). Accepted pairs persist as a VALUE_MAP transformation rule (top-level pairs config the engine applies at Generate Output) + learn into the Crosswalk Library for reuse. Frontend: relax the panel gate so AI normalization shows on any mapped field (not just LOV-constrained), add the AI method label. Graceful fallback to deterministic when AI is off."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
