@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Add 'Generate all & download (.zip)' button to the Conversion Objects panel header on the engagement overview - one click generates every object's FBDI output and downloads them together as a single load-ordered zip (reuses the existing download-all endpoint that already generates+zips). Sits alongside the existing top-bar button so it's discoverable where the objects are listed."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
