@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Setup wizard Review: file-mode aware. Source system shows 'File upload' (CSV/XLSX - Oracle Fusion FBDI), the Connection block shows the uploaded file name(s), and Implementation scope lists the FBDI conversions that will be created (e.g. the 6 supplier objects) with a live count. Green summary + counts updated for file mode."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
