@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Auto-seed the Oracle FBDI templates: bundle the 6 supplier templates + Customer master (HZ_IMP/RA_CUSTOMER, 19 sheets/1254 fields) + Item master (EGP_SYSTEM_ITEMS_INTERFACE, 17 sheets/1365 fields) in backend/app/data/fbdi_templates and parse them into FBDITemplate + FBDISheet + FBDIField on startup via the same parser as a manual upload (with a durable byte copy in Mongo). Idempotent + non-destructive: skips any template whose business object or name already exists, so the user's supplier templates are never duplicated. business_object is set explicitly (Supplier/Customer/Item...) so it lines up with the fan-out keyword match and the seeded mapping-catalog target_objects."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
