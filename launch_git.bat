@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "learn-from-example fixes: (1) PRESERVE gold's exact casing on learned constant defaults - values were being lowercased (Y->y, EMAIL->email, N->n) via _norm_val; now _clean keeps original case for what's written back while _norm_val is used only for value-set overlap scoring. (2) Capture NEAR-CONSTANT fields: when one value covers >=90% of the gold sample, learn it as a default (fixes Business Unit, Banks Delivery Channel/EMAIL/N constants, and the Bill-to BU that was mis-mapped to the supplier name). Re-upload gold + regenerate to apply."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
