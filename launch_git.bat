@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Wave P: supplier field-mapping seed no longer maps the Email Transactions flag directly to delivery fields. Removed 8 rows from supplier_field_mappings.json (Delivery Channel + Delivery Method on Supplier/Supplier Address/Supplier Site, plus Delivery Channel Code + Remit Delivery Method on Supplier Banks) that all sourced from Email Transactions as a plain 1:1 alias. Those plain aliases copied the Yes/No flag straight into the delivery fields (e.g. Delivery Method showing No on Supplier Import), competing with the CASE_WHEN derivation. Delivery Channel/Method derivation is now owned solely by supplier_transform_mappings.json: Delivery Channel CASE_WHEN on Import/Address/Site, Delivery Method CASE_WHEN on Site only, eBOS direct rows for its native columns. Verified live on the NetSuite and eBOS Fresh Test projects: Channel derives EMAIL for Email=Yes suppliers and blank for both-No on all three objects; Delivery Method only populates on Supplier Site and is blank on Import/Address; eBOS passes its native Delivery Method through on Site. JSON valid."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
