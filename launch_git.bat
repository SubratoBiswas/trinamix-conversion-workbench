@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Self-heal flat Item conversions at MAP time, not just startup. ROOT CAUSE of the still-flat output: ensure_item_multisheet runs ONLY at startup, so an Item conversion CREATED AFTER the last restart (e.g. a fresh 'EBOS - arena... Item Import' / 'SyteLine ERP Item Import') sits on a flat 26-column template the startup pass never saw, and generates a flat file. FIX: run_mapping_suggestions now calls the idempotent ensure_item_multisheet() at the START of AI Auto Map and refreshes the conversion, so a flat Item conversion is re-pointed onto the real 17-sheet template and its mappings build against the real interface sheets right before mapping -> Generate then emits the full multi-sheet FBDI. Idempotent no-op for conversions already on the real template. Combines with the earlier broadened _is_item (source-prefixed names) + heavy-template AI gate. After deploy, on the Item conversion: confirm Source Dataset is the correct file, click AI Auto Map (re-points + maps), then Generate -> 17-CSV ZIP, exact Oracle headers, hyphens preserved, no NULL text."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
