@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Issue 6 (duplicate target field on UI) + bundles staged Wave V.1. ISSUE 6 from QA: the same target field appears multiple times in Mapping Review (screenshot: 'Item Number' x4). Root cause is NOT duplicate records - 'Item Number' is the linking key on all 16 item interface sheets, so there are 16 legit fields sharing the name, each mapped to itemid. FIX: disambiguate by interface sheet. Backend /templates/{id}/fields now returns sheet_name per field (FBDIFieldOut + list_template_fields join sheet_id->name). Frontend badges a field with its sheet_name in the canvas AND table ONLY when the name occurs on more than one sheet, so per-sheet keys read as distinct instead of duplicated. Audit of the other 5 QA issues: their fixes are ALREADY in code/data (Fax Area Code PHONE_PART {part:area}; User Account Action VALUE_MAP No->'' + removed from _AUTHORITATIVE/_CONTROL_DEFAULTS; Invoice Match Option=Receipt default; Exchange Rate mapped; rejected->suggested export fixed via mapByTarget priority) - so the Fresh Test projects just need Re-run AI + Regenerate + verify against the DOWNLOADED file (not preview). Also carries staged Wave V.1 (CONDITIONAL_DATE both-schema + column-ref date tokens for Supplier Site Assignment Inactive Date)."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
