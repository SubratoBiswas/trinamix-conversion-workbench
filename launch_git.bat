@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Output file naming: name generated FBDI files after the object - fall back from the template business_object to the conversion target_object (e.g. 'Supplier Site', 'Supplier Banks') then the template name, and only use 'fbdi' as a last resort. Fixes files downloading as 'fbdi_<timestamp>.xlsx' when a template's business_object was blank."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
