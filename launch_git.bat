@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Convert-a-file (File upload) path: add the 'Implementation scope - Fusion Cloud modules' catalog (multi-select module cards + a count of default conversions that will be auto-created), matching the setup wizard. On Create, ProjectsApi.autoPopulate creates the default planned conversions for the selected modules in addition to the file-based fan-out. Create button now enables for files OR selected modules."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
