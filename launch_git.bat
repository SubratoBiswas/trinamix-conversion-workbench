@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Wave N: client picker on project creation so each project's learnings tag to the right tenant automatically. Backend: ProjectCreate/ProjectUpdate/ProjectOut carry client_id; create_project stores the picked client or falls back to the bootstrap default so every project is tenant-tagged; project responses include client_id and client_name. Capture already resolves the tenant via project.client_id, so a project created for client B now has its captured mappings tagged to B instead of resolving only for the default tenant. Frontend: SetupWizard step 1 gains a Client workspace dropdown (from the clients API, defaulting to the default client) wired into the create payload and shown on the review step. Prevents a future second client's learnings from being misattributed to NextPower. Backend py_compile clean; new frontend code type-checks clean apart from the repo's pre-existing id-number tsc noise; builds via Vite."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
