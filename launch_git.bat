@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Req2: FBDI load-sequence map on the Conversion Object detail screen AND at the top of Mapping Review - shows all the object's FBDI files (supplier=7) connected in Fusion load order, current file highlighted, generated ones linkable, missing ones flagged; on Mapping Review the chips also act as a per-file filter (click a file to review its mappings). Req6: FBDI-file dropdown now lists the FBDI file names (Supplier Import, Supplier Address, ...) instead of the source dataset name."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
