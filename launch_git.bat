@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Extend Gap-1 over-population suppression to Item Import. The Product Hub Item template is the same 18-sheet shape as Customer: one backbone interface table (EGP_SYSTEM_ITEMS_INTERFACE) plus ~16 optional child tables (revisions, categories, relationships, supplier, cost, EFF, trading partner...) that the gold leaves blank unless the source carries that content. Until now the backbone-only rule was Customer-only, so an item load wrote all rows into every child sheet with mostly-blank columns (junk Oracle's item import rejects). Generalized _sheet_carries_data: backbone (_ITEM_BACKBONE = EGP_SYSTEM_ITEMS_INTERFACE) always emits; every other item interface sheet emits data only when a real source column is actually mapped into it (e.g. a source Category column lights up EGP_ITEM_CATEGORIES), otherwise it's written headers-only. Verified against the uploaded ItemImportTemplate gold: main items sheet carries data, all 16 child sheets go headers-only. Customer behaviour unchanged; single-sheet objects unaffected."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
