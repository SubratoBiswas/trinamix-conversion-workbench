@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Smarter AI mapping (feature 2/4). Map target fields in concurrent, bounded batches (anthropic_suggest_batched): a single call over 156 targets overran max_tokens (truncated JSON -> silent rule-based fallback) and took 45s+; now targets are chunked (24) and run concurrently (5 at a time) via httpx.AsyncClient+asyncio.gather, each chunk well within the token budget and any failed chunk falling back to the deterministic mapper for just those targets. run_mapping_suggestions uses the batched path when the provider is anthropic; sample values + null% are already in the prompt. Backend-only, transparent to callers."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
