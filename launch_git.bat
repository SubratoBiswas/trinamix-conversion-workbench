@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Mapping table: add uncertainty flagging + CSV export. Rows the tool isn't confident about now carry a 'confirm' badge (low-confidence AI/weak-rule match, or a runner-up column that scored within 10 points - ambiguous); gold/learned rules and human overrides are never flagged since they're settled. Toolbar shows an 'N need confirmation' counter that filters to just those, plus Export CSV of the visible rows (source, target, required, how mapped, transform, confidence, status, needs-confirmation + why, alternatives with scores, notes)."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
