@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "AI data-quality + load-error explanations (features 3/4 & 4/4). (3) ai_quality_service: Claude reviews profiled source columns (type, null%, distinct, samples, pattern) vs the target object and flags data-quality issues + concrete fixes; appended into run_cleansing so they render in the existing cleansing UI (hallucinated columns dropped, graceful fallback). (4) ai_error_service: turns terse Oracle FBDI rejections into plain-English root cause + fix, deduped by message; auto-enriches LoadError.root_cause/suggested_fix at simulate time (zero frontend - existing Error Traceback UI shows them) plus a POST /load-runs/{id}/ai-explain to re-explain past runs. Both best-effort; deterministic path stands if AI is off."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
