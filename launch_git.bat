@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fan-out self-heals existing files: when a step matches a conversion that already exists (e.g. an ad-hoc Supplier Address at load order 100), it updates that conversion's planned_load_order and target_object to the correct step, so the load sequence + Next/Previous order stay correct instead of putting the pre-existing file last."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
