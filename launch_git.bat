@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fix load-sequence nav bugs: scope the file-set map + Previous/Next to the CURRENT engagement (global resolution was jumping users into older projects); Previous/Next now walk the engagement's files for the same object ordered by planned_load_order (robust vs template naming, no more wrongly-disabled Next); map lights a step when a same-object sibling matches by name OR load order."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
