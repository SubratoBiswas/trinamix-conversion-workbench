@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Mapping Review: add a Canvas/Table view toggle. New tabular view renders one row per target FBDI field - source column (with type/distinct/null profile), the target field with a REQ badge and data type, HOW it was mapped (Learned KB / Learned from gold / Rule-based / AI suggested / Rule-transform / Constant default / Suppressed / Manual override) plus any transform chip, the confidence %, the lower-probability alternative source columns from the ranked-candidates API (click one to re-map), and notes. Required fields with no source and no default are highlighted as gaps. Respects the existing filters and opens the inspector on row click."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
