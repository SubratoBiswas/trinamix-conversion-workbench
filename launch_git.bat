@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Broaden ensure_item_multisheet to catch source-prefixed flat Item templates. The prior repair only matched business_object=='item' or a short exact-name list, so a flat template named 'SyteLine ERP Item Import' (source-prefixed) was NOT recognized and still generated the flat 26-column CSV. _is_item now matches any item-MASTER template by name (an 'item' token + import/master) - incl. 'SyteLine ERP Item Import', 'NetSuite Item Master' - while explicitly EXCLUDING child item objects (cost, categories, revisions, relationships, associations, trading partner, structure, spec, attachment, cross-ref, price, uom) so they are never wrongly swept into the master repair. Confirmed the null-sentinel cleanse already deployed (SyteLine output NULL count now 0). After deploy the startup repair re-points the SyteLine item conversion onto the real 17-sheet template and clears its mappings; then Re-run AI mapping (applies the fixed no-hyphen-strip logic + the seeded standard-field mappings) and Generate -> 18-sheet ZIP with exact Oracle headers, hyphens preserved (50-1020, SH-2.10.RBK...), no NULL text."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
