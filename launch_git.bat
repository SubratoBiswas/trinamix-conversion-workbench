@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Uploaded gold / prompt now overrides the aggressive AI mapping. learn-from-example: fields the gold leaves BLANK are now SUPPRESSED (status not_applicable) instead of skipped, and saved as reusable kind=suppress_field learned rules; the AI's over-mapping for those fields is overridden. apply_learned_to_conversion applies suppress_field over any still-suggested mapping. Output keeps suppressed fields blank (skips control defaults + authoritative constants for them). Steer prompts gain a suppression directive ('clear X' / 'leave X blank' / \"don't map X\"). So an uploaded gold example or a prompt makes the tool populate exactly what the user's gold populates and leave blank what it leaves blank."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
