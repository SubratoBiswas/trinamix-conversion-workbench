@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Learning capture: lower the auto-capture confidence bar 0.85->0.60 so confident rule-based/AI mappings are captured (not just user-approved), making the learning DB actually compound after each successful output. Gold/prompt/accepted rules are still never downgraded by auto-capture."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
