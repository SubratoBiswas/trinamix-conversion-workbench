@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__ 1>nul 2>nul
echo Staging...
git add -A
echo Committing (no-op if nothing changed)...
git commit -m "CW_Issues #1-#4 + AI features + filled Oracle templates + banded mapping export. Multi-sheet upload prompt (peek-sheets, per-sheet CSV extract, picker modal); higher mapping confidence via better NetSuite tokenization; approved effective-defaults now applied at generation; banded Excel mapping export; AI data-intelligence suite (entity resolution, anomaly detection, cross-client suggestions, readiness scoring, synthetic data), grounded copilot and agentic plan-step; filled-in Oracle FBDI Excel templates alongside CSV; efficient merged download-all. Full detail in docs/SESSION_HANDOFF.md section 9. Backend py_compile clean; 66 pure unit tests pass."
echo Pushing...
git push origin main
echo.
echo ====== DONE - review the messages above, then press any key to close ======
pause
