@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Persistent file storage: uploaded templates/datasets now write to a persistent disk (auto-detects DATA_DIR env or a Render Persistent Disk mounted at /var/data) instead of the ephemeral container disk that gets wiped on every redeploy - so re-parse and stored files survive deploys. Falls back to local ./uploads when no disk is present."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
