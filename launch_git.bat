@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "AI data-quality (feature 3) reliability: scope the review to mapped columns first + cap at 35 (was first-60 of 258 -> oversized prompt that returned nothing on wide datasets), and extract the JSON array even if the model wraps it in prose. Deterministic cleansing already surfaces 186 real issues; this makes the AI-flagged issues actually come back."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
