@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fix run_cleansing to materialize the durable GridFS dataset copy before parsing (Render disk is ephemeral, so dataset.file_path was gone after redeploy -> empty dataframe -> no cleansing issues and the AI data-quality review was skipped). Now cleansing (rule-based + AI) actually runs on the real data."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
