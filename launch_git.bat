@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Supplier is 6 FBDI files, not 7: Supplier Contact Addresses is a second worksheet inside the Supplier Contacts template (2-sheet workbook), not a standalone template - removed the phantom 7th step from the fan-out catalog so it stops reporting a missing template and the map/set show 6."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
