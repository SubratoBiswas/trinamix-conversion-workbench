@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Reusable gold learning: learn-from-example now saves each discovered source->target map as an OBJECT-LEVEL LearnedMapping (kind=column_mapping) - the exact shape apply_learned_to_conversion re-reads during auto-map. So once you learn an object from a gold example, ANY new engagement of that object auto-applies the mappings on Generate Set (no re-teaching). Constant defaults recorded as example_default (common ones also applied in code)."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
