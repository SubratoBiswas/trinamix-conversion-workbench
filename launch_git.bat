@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "AI model selector for cost control. Global dropdown in the top bar lets the user pick the Anthropic model (Claude Haiku 4.5 = cheapest/fastest -> Sonnet 4.6 balanced -> Opus 4.8 = most capable/priciest); the choice applies to every AI feature (mapping, crosswalks, defaults, data-quality, error explanations). Backend: AppSetting singleton persists the choice, /api/settings/ai-model GET/PUT, and startup reloads it into settings.ANTHROPIC_MODEL which every AI service already reads (so no per-service change). Persists across restarts."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
