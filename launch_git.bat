@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Authoritative control-field defaults: gold-constant fields (Import Action, Address Name, Supplier Site, User Account Action, Business Relationship, Supplier Type, Tax Org Type, Federal reportable, Delivery Channel, Pay/Ordering/RFQ) and the running Supplier Number key are now written to their standard value EVEN IF auto-map filled the column with a wrong source guess (previously defaults only filled blanks, so wrong guesses like Address Name=street, Supplier Site='No', User Account Action=phone stuck). Genuinely source-mapped fields (names, tax ids, web site, addresses) are untouched; per-row fields (Business Unit, currency) remain mapped/defaulted."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
