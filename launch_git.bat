@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fix LLM mapping never taking effect: _parse_response did int(target_field_id) but target ids are Mongo ObjectId strings (24-char hex), so json parsing raised on every item and the AnthropicMapper silently fell back to rule-based for all mappings. Compare ids as strings, skip unknown/hallucinated target ids. This makes AI (Sonnet) source->target mapping actually apply for the first time."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
