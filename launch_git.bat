@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Output now matches Oracle gold templates: (1) preserve exact template headers incl. '*' required markers (parser keeps raw header; output falls back to appending '*' for required fields); (2) expanded smart-defaults to the confirmed supplier constants - Business Relationship=PROSPECTIVE, Federal reportable=N, Delivery Channel=EMAIL, Address Name=PRIMARY, Pay/Ordering/RFQ=Y, Supplier Site=PRIMARY, Client/Procurement/Bill-to BU, Administrative Contact=Y, User Account Action=NONE, sequential Supplier Number from 100000; applied only to columns the source left blank."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
