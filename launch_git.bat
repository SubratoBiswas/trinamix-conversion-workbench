@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Conversion Objects: add 'Upload gold outputs' (bulk). Multi-file picker matches each gold file to its object by filename (assignment/address/contact/bank/site/import) and applies learn-from-example to the matching conversion, so uploading all 6 gold files at once teaches every conversion what to map/default/leave-blank. Per-file status (object + mapped/defaults/blanked counts) shown under the header. Frontend-only, reuses the existing learn-from-example endpoint."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
