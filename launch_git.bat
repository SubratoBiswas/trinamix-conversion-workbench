@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Waves P + Q. (P) Supplier field-mapping seed no longer maps the Email Transactions flag directly to delivery fields: removed 8 rows from supplier_field_mappings.json (Delivery Channel + Delivery Method on Supplier/Supplier Address/Supplier Site, plus Delivery Channel Code + Remit Delivery Method on Supplier Banks) that sourced from Email Transactions as a plain 1:1 alias and copied the Yes/No flag straight into the delivery fields. Delivery derivation is now owned solely by supplier_transform_mappings.json (Delivery Channel CASE_WHEN on Import/Address/Site, Delivery Method CASE_WHEN on Site only, eBOS direct rows for native columns). (Q) BOM / Item Structure mappings for NextPower: new bom_field_mappings.json + seed_bom_field_mappings() wired into startup. Seeds the analyst Tracker + eBOS BOM crosswalk to the Item Structure workbook (EGP_STRUCTURES/COMPONENTS_INTERFACE) - Item Name, Component Item Name, Structure Item Name, Item Sequence, Quantity for both Arena vocabularies - plus the fixed constant defaults (Transaction Type=SYNC, Structure Name=Primary, Organization Code=NXT_ITEM_ORG) as example_default learnings, all scoped to the NextPower client and idempotent. Verified live: BOM Tracker (2723 rows) and BOM eBOS (3045 rows) projects auto-map from the learnings and generate into the 307-field Item Structure template with the mapped item/component values and the SYNC/Primary/NXT_ITEM_ORG constants populated. The Item Structure workbook is now BUNDLED in the repo (data/fbdi_templates/BOMItemStructure_EGP_STRUCTURES_INT.xlsm) with a dedicated ensure_bom_multisheet() startup seed that force-seeds the real 4-sheet template (EGP_STRUCTURES/COMPONENTS/SUB_COMPS/REF_DESGS_INTERFACE) even when the thin BomImport already holds the BOM object, and re-points any BOM conversion off the thin template - so a from-scratch database rebuilds BOM automatically. Backend py_compile clean; JSON valid."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
