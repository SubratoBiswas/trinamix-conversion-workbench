@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fold gold into Generate & download - remove the separate 'Apply gold now' button. Generate all & download now runs the full layered pipeline per object: gold reference standard (if on file, force-applied at generate) + learnings + uploaded templates + deterministic Python (country/currency/UOM/flags) + rule-based matching, with AI only for the residual. Banner + button copy and live status updated to reflect the one-click bundle."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
