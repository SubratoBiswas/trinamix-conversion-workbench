@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fix: move the FBDI template + mapping-catalog seeds OFF the startup path. Parsing the bundled workbooks (Item alone is 17 sheets / ~1.4k columns) and inserting thousands of field rows inside lifespan delayed readiness and made the first requests after a deploy time out (login hung). Now fired as a background task (asyncio.create_task) so the app serves immediately and the seeds populate a moment later; both are idempotent so a restart mid-seed is safe. Also collapsed the template-existence check to a single collection scan instead of one per bundled template."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
