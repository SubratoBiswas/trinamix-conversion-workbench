@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Convert flow now fans out the full FBDI set: creating conversions for a multi-file object (supplier/customer/item/...) generates the whole load sequence from step 1 (e.g. Supplier Import) instead of the single file the AI detected - fixes engagements ending up with just 1 conversion (Supplier Address) where Next was disabled and step 1 was skipped. Falls back to single create for non-set objects."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
