@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Setup wizard file-upload mode: hide the DB Source-connection form entirely when the Custom/Other file-upload path is chosen (show only the upload card), and stop gating Continue on DB credentials - Continue enables once uploads finish analyzing. File-mode engagements persist a clean mock connection."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
