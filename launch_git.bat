@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Customer FBDI: fix the flat-output gap (structure + linkage). Root cause: the Customer conversion targeted a FLAT synthetic template (single 'Import' sheet from the built-in standard fields), not the real 19-sheet Oracle Customer Import (HZ_IMP interface tables). Gold/AI/learnings fill VALUES into whatever shape they're given - they can't turn a flat sheet into 19 linked tables, so the output stayed flat no matter what. Two-part fix. (1) Re-point: new POST /fbdi/customer/repoint moves Customer conversions off the flat template onto the real multi-sheet one and re-runs mapping so fields line up; 'Fix Customer target' button on the FBDI Templates page. (2) Linkage: new customer_structure_service generates the glue no source extract provides - Batch Identifier, the Party/Customer-Account/Account-Site Original System keys, a per-customer reference that flows consistently from parent to child, and Oracle's documented per-level sentinels (party level: Customer Account = -1, Account Site blank; account level: account set, site blank; site level: both) - wired into generate_output_artifact for the Customer object and verified against the real template's actual column names across PARTIES/ACCOUNTS/ACCTSITES/PROFILES. Stated honestly in code + to the user: this ships the correct STRUCTURE and the linkage foundation for an account-level load; fanning each customer into multiple party-site/account-site/site-use/profile child rows and final validation against a specific Fusion instance is the remaining work, not a one-shot."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
