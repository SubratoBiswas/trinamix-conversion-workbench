@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Phase 2 learning UI: add a 'Learn from example & steer' panel to the Mapping Review screen (LearnFromExamplePanel) with an example-output file upload and a prompt textarea, plus ConversionsApi.learnFromExample client method. Posts to /learn-from-example, shows inferred mappings/defaults + applied prompt directives, then reloads the mappings so the user can Generate Output."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
