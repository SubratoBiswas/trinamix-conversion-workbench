@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Large-file generation: (1) chunked transform - process the source in 25k-row windows and concat (row-local, byte-identical output; bounds peak memory for tall/wide extracts); (2) non-blocking - run each chunk transform via asyncio.to_thread so generation never blocks the event loop; (3) parallel objects - Generate all runs the 6 objects with a concurrency pool instead of sequentially, plus a live per-object progress panel (queued/mapping/generating/done/failed, overall bar + %)."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
