@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Mapping table: promote Required to its own sortable column. The req sort was hidden as a chip inside the Target header; it's now a first-class 'Required' column (required / optional) with a click-to-sort header, and ascending puts MANDATORY fields first (what people actually mean by sort-by-required). Removed the duplicate inline REQ badge from the target cell."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
