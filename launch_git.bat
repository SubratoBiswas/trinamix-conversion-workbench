@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fixed-value feature: pin a constant into any field. Adds a 'Fixed value' control in the Mapping Inspector - the user types a constant (e.g. AE, CORPORATION, SPEND_AUTHORIZED); the tool clears the source column, sets default_value, and marks the row overridden so the constant survives gold/AI/learning and is written into every output row. A Clear button removes it. Backend PUT /mappings/{id} already supports this via MappingUpdate (source_column=null, default_value, status) with exclude_unset - no backend change needed. Directly serves the supplier analyst gaps: Tax Organization Type=CORPORATION, Supplier Type=SUPPLIER, Business Relationship=SPEND_AUTHORIZED, plus hand-maintained country/codes on any object."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
