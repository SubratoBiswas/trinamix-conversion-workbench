@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Gap 1: stop over-populating Customer interface sheets. Verified the generated 19-sheet output against the NCR/BATCH gold: structure + linkage were right, but the naive fan-out wrote 5599 rows into ALL 19 sheets, so optional entity tables (Relationships, Roles, Classifications, Account Contacts, Pay Method) held thousands of rows of pure linkage glue with no real content - junk Fusion would reject. Gold populates only the party/account/site/profile backbone (8 sheets). New rule in generate: for the Customer object, the backbone (Parties, PartySites, PartySiteUses, Locations, Accounts, AcctSites, AcctSiteUses, Profiles) always emits; every other sheet emits data ONLY when a real source column is actually mapped into it (checked against the mappings, not the glue). Suppressed sheets are still written headers-only (0 rows) so the file set stays complete, like the empty tabs in a hand-filled template. Result on this source: backbone + Contact Points + Banks carry data (the source genuinely has emails/phones/bank details the NCR gold didn't), the pure-glue sheets go empty. Non-customer objects are unaffected."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
