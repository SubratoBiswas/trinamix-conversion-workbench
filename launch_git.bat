@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Durable fix for learned-mapping reset: (1) _reseed_scm_om_templates no longer force-reseeds NON-empty SCM/OM templates on startup - that delete+reinsert gave every field a new ObjectId on each redeploy, orphaning approved/learned mappings and replacing uploaded gold multi-sheet templates (e.g. Supplier Site = POZ_SUPPLIER_SITES_INT + Third_Party) with the generic single-'Import' schema; now only EMPTY templates are seeded, ids preserved. (2) auto_seed_if_empty refuses to seed the generic schema over any template that has durable uploaded bytes (reparse restores its real structure instead). Learned mappings + gold templates now survive redeploys."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
