@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fix reset-defaults 422: derive the object like the learning engine does. The endpoint keyed off conv.target_object alone and 422'd ('Conversion has no target object') whenever the object lived only on the template - which is exactly the Customer case in front of the user, so every reset threw the generic 'Couldn't reset the defaults' toast. Now it uses _business_object_for(conv) = template.business_object or conv.target_object, the same derivation the engine uses when it STORES the example_default rules, so the object always matches what needs forgetting. run_mapping_suggestions (the rerun) already persists and re-applies learned rules minus the ones just deleted, so removing the gold default and re-running AI now works end to end."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
