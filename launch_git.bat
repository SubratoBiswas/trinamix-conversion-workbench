@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Mapping table: filters + sorting, required-first. Toolbar gains a source/target search, a 'how it's mapped' method dropdown, a Required-only toggle, the needs-confirmation toggle and a Reset. Columns (source / target / how mapped / confidence, plus a req chip) are click-to-sort with direction. Default order now puts MANDATORY fields on top, then template sequence - and required stays above optional as the tiebreak in every sort. FIX rule-preview 'Network Error': the preview endpoint read the source via the raw ds.file_path (gone after a redeploy - the disk is ephemeral) and parsed the ENTIRE extract for a 5-row sample on every keystroke, which could kill the worker and drop the connection. It now rehydrates from GridFS and reads only nrows=n, and never 500s; the UI reports an unreachable backend plainly instead of a bare 'Network Error'."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
