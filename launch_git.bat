@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Mapping import: widen the object picker. It was populated only from objects that already had learned rules, so it showed a near-random handful. New /learned-mappings/known-objects returns the union of a curated canonical Oracle Fusion FBDI object list (59 objects across 10 modules - Procurement, Inventory/SCM, Order Management, Receivables, Payables, GL, Assets, Cash Management, Projects, HCM), the business objects of every loaded template (authoritative - what the tool can actually target), and every object already carrying a learned rule. De-duped case-insensitively with canonical casing winning. The field stays free-text, so this is suggestion-only - breadth is pure upside and a consultant can still type an object that isn't listed."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
