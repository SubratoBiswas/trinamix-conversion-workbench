@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Broaden ensure_item_multisheet to catch source-prefixed flat Item templates. The prior repair only matched business_object=='item' or a short exact-name list, so a flat template named 'SyteLine ERP Item Import' (source-prefixed) was NOT recognized and still generated the flat 26-column CSV. _is_item now matches any item-MASTER template by name (an 'item' token + import/master) - incl. 'SyteLine ERP Item Import', 'NetSuite Item Master' - while explicitly EXCLUDING child item objects (cost, categories, revisions, relationships, associations, trading partner, structure, spec, attachment, cross-ref, price, uom) so they are never wrongly swept into the master repair. Confirmed the null-sentinel cleanse already deployed (SyteLine output NULL count now 0). PLUS: AI Auto Map heavy-template gate. On the re-pointed 1365-field Item template (and 1250-field Customer), ~1200 structural/EFF slots have NO source column, so the whole residual was sent to the LLM in one sync batch, blowing the ~100s gateway -> mapping request failed with 0 mappings saved ('not mapping at all'). run_mapping_suggestions now sets _heavy=len(targets)>300 and SKIPS the AI residual for heavy templates, resolving deterministically + from learnings/gold only (sourceless slots correctly stay gaps). After deploy: click AI Auto Map (now fast, saves all 1365 mappings with standard fields mapped via rule + the 53 seeded learnings + gold), then Generate -> 18-sheet ZIP, exact Oracle headers, hyphens preserved (50-1020, SH-2.10.RBK...), no NULL text."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
