@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Compounding learning: every successful output auto-captures the conversion's trustworthy mappings/defaults/suppressions as reusable object-level LearnedMappings (capture_learnings_from_conversion, called from generate_output_artifact). Only user-approved/overridden, high-confidence (>=0.85), or suppressed signals are captured, and auto-capture never downgrades a gold/prompt/accepted rule. Mapping is now learning-first: targets already covered by a learned rule are resolved from the learning DB and skipped from the AI weak-set entirely, so the tool needs less AI as it accumulates learnings. Priority: uploaded templates + learning DB + gold + Python -> AI only for the true residual; quality preserved via graceful fallback."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
