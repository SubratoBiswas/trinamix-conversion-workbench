@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Mapping Review: add a 'Generate & download' button in the header (next to Recommendations / Re-run AI). Generates this conversion's FBDI output and downloads the file (uses the artifact's real file_name, so multi-sheet objects come as a .zip). Disabled until at least one field is mapped."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
