@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Convert-a-file page: (1) gate the Fusion module catalog to appear only after a file is uploaded; (2) replace the plain progress pills with a wizard-style stepper (Upload files - Detect source & target - Review & scope - Create & map) with circular numbered steps, checkmarks, and green connectors, matching the setup wizard."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
