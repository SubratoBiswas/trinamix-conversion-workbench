@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Mapping Review: show unmapped control fields as 'defaulted -> value' instead of a red required gap. New backend GET /api/conversions/{id}/effective-defaults merges static control constants + sequence keys + learned example defaults, and (when AI_PROVIDER is set) dynamically asks the LLM to infer constants for any remaining required-unmapped field, caching the result as a reusable example_default. Frontend fetches this, badges those fields green (Defaulted -> CREATE/PROSPECTIVE/900001/...), and excludes them from the required-gaps count. Graceful fallback to deterministic defaults when AI is off."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
