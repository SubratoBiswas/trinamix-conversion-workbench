@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Surface silent durable-copy failures on upload. Investigated the reported '1000-row upload truncation': the tool does NOT cap uploads - profiling samples 3000 rows but count_data_rows records the true full count, the whole raw file is stored in GridFS, and generation reads the entire file (max_rows=None). No 1000-row limit anywhere in the upload/parse/generate path. The one real weakness: create_dataset_from_upload wrapped the GridFS durable-copy in a bare except: pass, so if a large file failed to store the dataset still profiled but a later Generate could not find the source ('re-upload the dataset') - which can look like a truncated/failed upload. Now it logs the exception (with dataset id + row count) instead of swallowing it, so a partial/failed big-file upload is diagnosable. Bundles the issue-6 sheet-name disambiguation if not already pushed."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
