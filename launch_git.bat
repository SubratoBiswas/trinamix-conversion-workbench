@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Extend output smart-defaults to Customer + Item (object-aware, matched by exact column name so no cross-contamination): Item Transaction Type=SYNC / Item Class default; Customer Insert-Update flag + auto-numbered Party/Account/Customer numbers; supplier defaults unchanged. (Supplier Contacts 0-fields was a stale seed, not a parser bug - parser verified to extract 97 fields; needs a template re-parse.)"
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
