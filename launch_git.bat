@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Seed analyst Supplier mappings (NetSuite SS Vendors + Arena eBOS) + value-map the 3 constant fields. (A) supplier_field_mappings.json: 138 analyst-confirmed 1:1 mappings from the NextPower Supplier Field Mapping doc v3, across all 6 supplier interface objects (Supplier, Address, Site, Site Assignment, Contacts, Banks), both sources - seeded as idempotent column_mapping learnings via new seed_supplier_field_mappings(); transformation-flagged rows (coalesce, phone-parse, subsidiary->BU) intentionally left for the crosswalk layer. (B) Tax Organization Type / Supplier Type / Business Relationship are no longer forced constants: removed from _AUTHORITATIVE so a mapped+crosswalked source value wins (Entity Type->Tax Org Type, Category->Supplier Type, Vendor Approval Status->Business Relationship with VALUE_MAP Approved->SPEND_AUTHORIZED else PROSPECTIVE); the CORPORATION/SUPPLIER/SPEND_AUTHORIZED constants stay in _CONTROL_DEFAULTS as fallback when the source is blank. catalog seeder now honors per-row rule_type/rule_config so the Business Relationship value-map is seeded. Bundles the earlier map-time Item self-heal if not already pushed."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
