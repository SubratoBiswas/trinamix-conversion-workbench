@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Phase 2 learning feature: POST /api/conversions/{id}/learn-from-example accepts a populated example output and/or a plain-text steering prompt. It infers source->target mappings + constant defaults from the example by value-set overlap (robust to row reordering; e.g. Supplier Name->Name, Corporate Web Site->Web Address), and applies prompt directives like 'default Business Relationship to PROSPECTIVE' / 'map Supplier Name from VENDOR_NAME'. Inferred rules are written as approved mappings so Generate Output reproduces the example, and saved as reusable LearnedMapping reference standards per object."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
