@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Harden AI data-quality (feature 3): match the model's field_name to source columns by normalized name (was exact-match, which silently dropped every AI issue when the model echoed a slightly different name/casing), and raise max_tokens 2500->4000 so the JSON array of issues isn't truncated. The deterministic cleansing (186 real issues) already works after the materialize fix; this makes the AI-flagged issues actually surface."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
