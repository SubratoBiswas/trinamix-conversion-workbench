@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Generate all & download (.zip): run the full pipeline client-side with live per-object status. Phase 1 AI auto-maps every conversion that has no source-mapped fields (skips already-mapped ones), phase 2 generates each object's FBDI output, phase 3 zips + downloads. The button now shows the actual step ('AI auto-mapping - Supplier Address (2/6)', 'Generating output - Supplier Import', 'Packaging 6 files into .zip') instead of a bare spinner. New backend GET /conversions/project/{id}/download-zip packages the already-generated outputs (no re-generation) so phase 3 is fast."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
