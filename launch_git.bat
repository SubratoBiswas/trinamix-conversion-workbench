@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fix reset-defaults NameError: import MappingSuggestion. The endpoint queried MappingSuggestion.find(...) but only imported FBDIField and LearnedMapping locally, so it raised 'name MappingSuggestion is not defined' - which the error-surfacing wrapper from the last commit correctly reported in the toast (working as intended). Added the missing local import. All other names in the function are module-level or already imported."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
