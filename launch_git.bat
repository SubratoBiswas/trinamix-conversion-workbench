@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Reusable gold reference standards: once gold is applied for an object type it is stored globally and auto-applied to every future conversion of that object - no re-upload needed. Backend: apply_learned_to_conversion now also re-applies learned constant defaults (example_default), plus new GET /learned-mappings/reference-standards summary. Frontend: persistent 'reference standards on file' banner + per-object 'gold on file (Nm Md Ks)' badge on the Conversion Objects screen, and a matching auto-applied chip on the single-conversion Mapping Review. Re-uploading gold still overrides the stored standard."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
