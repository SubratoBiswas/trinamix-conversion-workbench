@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Item Import: over-population suppression + analyst standard-field mappings. (A) Extend Gap-1 backbone-only rule to Item: EGP_SYSTEM_ITEMS_INTERFACE always emits; the ~16 child tables (revisions, categories, relationships, supplier, cost, EFF, trading partner) emit only when a real source column is mapped into them, else headers-only - matching the ItemImportTemplate gold (main sheet=data, 16 child sheets=empty). Customer/single-sheet objects unchanged. (B) Seed 53 analyst-confirmed Item standard-field mappings from the NXT Item Field Mapping Document (Arena EBOS/Ratana Lee/Anaplan, SyteLine, NetSuite -> Item Number, Description, Primary UOM, Revision, Lifecycle Phase, Make or Buy, Item Class, Unit Weight, lead times, etc.) as idempotent column_mapping learnings via a new item_field_mappings.json + seed_item_field_mappings(); they auto-apply on any Item conversion regardless of which source system the file came from. EFF (the larger, extensible-flexfield portion) scoped separately in EFF_Support_Scope.md."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
