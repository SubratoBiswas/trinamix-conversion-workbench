@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Conversion detail page: add per-object gold upload ('Upload gold' / 'Replace gold') that learns + stores the reusable reference standard and is force-applied at generate (same logic as the project screen). Show a 'gold reference standard on file' banner with counts when a standard exists for this object - whether uploaded here or on the project page."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
