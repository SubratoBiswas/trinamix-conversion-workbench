@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Requirements wave: (Req1) fan-out engine - one dataset generates all FBDI templates for a conversion object (supplier/customer/item/AP/AR/GL catalog), auto-mapped + load-sequenced, missing templates reported; generate-set + object-types endpoints; Generate-set button on Convert page. (Req8) exact FBDI CSV output - all interface columns in template sequence, real headers, blanks for unmapped, no instruction rows. (Req6) FBDI-file target filter relabeled. (Req9) fixed mapping-list back nav + Back-to-project link + engagement param. (Req10) NetSuite + Infor SyteLine prioritized over EBS."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
