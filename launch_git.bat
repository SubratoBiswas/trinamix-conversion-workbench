@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Wave M: Customer and Employee HDL fan-out fixes. Single-workbook FBDI objects Customer Item AP AR GL now resolve to the full multi-sheet workbook instead of a same-named stub template. Over-population guard extended to Customer with a numeric-to-flag-field drop so an amount like Credit Limit cannot land in the Credit Hold flag. Verified live on the NetSuite Customer and Workday Employee HDL projects."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
