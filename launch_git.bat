@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Output now matches the reference gold-standard: generate populated .xlsx template workbooks (interface sheet(s) with headers + data, multi-sheet templates get one tab per sheet) instead of stripped CSVs; and add a smart-defaults layer that fills unmapped control columns (Import Action=CREATE, Batch ID, Tax Org Type, Supplier Type) and auto-generates a sequential Supplier Number - so a supplier extract produces the same load-ready files as the manual LLM output. Unit-tested against the All-Vendors input / Suppliers-FBDI reference pair."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
