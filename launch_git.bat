@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Fixed-value UI + supplier analyst gap fixes. (1) Fixed value control in the Mapping Inspector - type a constant (AE, CORPORATION, SPEND_AUTHORIZED); it clears the source column, sets default_value, marks the row overridden so the constant survives gold/AI/learning into every row; Clear removes it. (2) Supplier defaults corrected to Oracle lookup codes: Tax Org Type CORPORATION, Supplier Type SUPPLIER, Business Relationship SPEND_AUTHORIZED (were Corporation/Supplier/PROSPECTIVE). (3) Supplier Number now keeps the mapped legacy 'Number' column and only auto-numbers blank rows (customer party/account keys still use the authoritative running sequence the 19-sheet linkage needs). (4) Matcher vocab + exact-identity bonus so Taxpayer ID<-Taxpayer ID, Payment Method<-Vendor Payment Method, Remittance Fax<-Fax, Tax Registration<-Tax Number, and a literal 'Number' column wins Supplier Number over a foreign-system 'Coupa Supplier ID'. E-mail tokenization normalized so 'Remittance E-mail' matches an 'Email' source. Parent Supplier left unmapped per analyst (needs business input)."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
