@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "AI: use latest Claude Sonnet (claude-sonnet-4-6). Set config ANTHROPIC_MODEL default to claude-sonnet-4-6 (was the older claude-sonnet-4-20250514) so the mapping provider and copilot chat use latest Sonnet, and point defaults_service AI inference at ANTHROPIC_MODEL (claude-sonnet-4-6) instead of haiku. Key on Render was refreshed by the user."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
