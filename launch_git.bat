@echo off
cd /d "C:\Users\SubratoBiswas\trinamix-conversion-workbench"
echo Cleaning any git locks...
del /f /q ".git\index.lock" 2>nul
del /f /q ".git\HEAD.lock" 2>nul
echo Dropping tracked bytecode...
git rm -r --cached --ignore-unmatch backend/app/__pycache__ backend/app/services/__pycache__ backend/app/parsers/__pycache__
echo Staging...
git add -A
echo Committing (no-op if clean)...
git commit -m "Follow-ups: (1) Customer/Item/AP/AR/GL are single multi-sheet FBDI workbooks - fan-out catalog now resolves each to its one template (one conversion) instead of reporting sub-objects missing; supplier stays multi-file. (2) Multi-sheet output - generate_output_artifact emits one CSV per interface sheet (all sheets populated, numbered in load order) bundled as a zip for multi-sheet templates; single-sheet stays one CSV; download-all uses each artifact's real extension."
echo Pushing...
git push origin main
echo.
echo ====== DONE - Press any key to close ======
pause
